// Example: Functions with Early Return
fn absolute(x: Int) {
    if (x < 0) {
        return -x;
    };
    x
}

fn twice<T>(x: Fn<T, T>) {
    x(x(x))
}

fn max(a: Int, b: Int) {
    if (a > b) { a } else { b }
}

fn min(a: Int, b: Int) {
    if (a < b) { a } else { b }
}

fn main() {
    print("absolute(42)  = " + absolute(42));
    print("absolute(-42) = " + absolute(-42));
    print("max(3, 7)     = " + max(3, 7));
    print("min(3, 7)     = " + min(3, 7));
}
