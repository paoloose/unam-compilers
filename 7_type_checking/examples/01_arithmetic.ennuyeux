fn print<T>(what: T) {
    // compiler implemented
}

// Example: Basic Arithmetic and Variables
fn main() {
    let a: int = 10;
    let b: int = 3;

    print("a + b = " + (a + b));
    print("a - b = " + (a - b));
    print("a * b = " + (a * b));
    print("a / b = " + (a / b));
    print("a % b = " + (a % b));
    print("a ** b = " + (a ** b));

    // Unary negation
    let neg = -a;
    print("neg = " + neg);
}
